<!DOCTYPE html>
<html lang="de">
<head>
<meta charset="utf-8" />
<meta name="viewport" content="width=device-width,initial-scale=1" />
<title>Aurelux Watches — Luxuriöse Zeitmesser</title>
<link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
<link href="https://fonts.googleapis.com/css2?family=Playfair+Display:wght@400;600;700&family=Inter:wght@300;400;600;700&display=swap" rel="stylesheet">
<style>
  :root{
    --bg:#0f1113;
    --card:#0f1416;
    --muted:#bfc7cd;
    --gold:#c9a24a;
    --accent:#e6d8b0;
    --glass: rgba(255,255,255,0.03);
    --radius:14px;
    --maxw:1100px;
  }
  *{box-sizing:border-box}
  html,body{height:100%}
  body{
    margin:0;
    font-family: "Inter", system-ui, -apple-system, "Segoe UI", Roboto, "Helvetica Neue", Arial;
    background: linear-gradient(180deg, #070708 0%, #0f1113 100%);
    color: #e9eef2;
    -webkit-font-smoothing:antialiased;
    -moz-osx-font-smoothing:grayscale;
    padding:24px;
    display:flex;
    justify-content:center;
  }

  .site{
    width:100%;
    max-width:var(--maxw);
  }

  /* Header */
  header{
    display:flex;
    align-items:center;
    gap:18px;
    justify-content:space-between;
    margin-bottom:28px;
  }
  .brand{
    display:flex;
    align-items:center;
    gap:14px;
  }
  .logo {
    width:64px;height:64px;border-radius:12px;
    background:linear-gradient(135deg, rgba(201,162,74,0.12), rgba(201,162,74,0.25));
    display:flex;align-items:center;justify-content:center;
    box-shadow: 0 8px 30px rgba(0,0,0,0.6), inset 0 -6px 18px rgba(255,255,255,0.02);
    border: 1px solid rgba(255,255,255,0.04);
  }
  .logo span{ color:var(--gold); font-family:"Playfair Display"; font-weight:700; font-size:20px; letter-spacing:1px; }
  .brand h1{ margin:0; font-family:"Playfair Display"; font-weight:700; font-size:20px; color:var(--accent) }
  .brand p{ margin:0; font-size:12px; color:var(--muted) }

  .header-actions{
    display:flex;
    gap:12px;
    align-items:center;
  }
  .search {
    background:var(--glass);
    border-radius:10px;
    padding:8px 10px;
    display:flex;align-items:center;gap:8px;
    border:1px solid rgba(255,255,255,0.03);
  }
  .search input{
    background:transparent;border:none;outline:none;color:var(--accent);font-size:14px;
    width:180px;
  }
  .btn {
    background: linear-gradient(180deg, rgba(201,162,74,0.15), rgba(201,162,74,0.06));
    color:var(--gold);
    border: 1px solid rgba(201,162,74,0.18);
    padding:10px 14px;border-radius:10px;font-weight:600;cursor:pointer;
    box-shadow:0 8px 30px rgba(0,0,0,0.6);
  }
  .btn.secondary{
    background:transparent;color:var(--muted);border:1px solid rgba(255,255,255,0.04);
  }
  .cart-count{
    background:var(--gold); color:#0b0b0b; font-weight:700; border-radius:8px; padding:4px 8px; font-size:13px;
  }

  /* Hero */
  .hero{
    display:grid;
    grid-template-columns: 1fr 420px;
    gap:24px;
    margin-bottom:28px;
  }
  .hero-card{
    background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
    border-radius:var(--radius);
    padding:28px;
    box-shadow: 0 20px 60px rgba(0,0,0,0.6);
    border:1px solid rgba(255,255,255,0.03);
  }
  .hero-card h2{
    margin:0 0 8px 0;font-family:"Playfair Display";font-size:32px;color:var(--gold);
  }
  .hero-card p{ margin:0 0 18px 0; color:var(--muted); font-size:15px; line-height:1.5 }
  .hero-cta{ display:flex; gap:12px; flex-wrap:wrap; }
  .hero-image{
    border-radius:var(--radius);
    overflow:hidden;
    box-shadow: 0 20px 60px rgba(0,0,0,0.6);
    border: 1px solid rgba(255,255,255,0.03);
  }
  .hero-image img{ display:block; width:100%; height:100%; object-fit:cover; max-height:420px; }

  /* Products grid */
    .products{
      margin-top:24px;
      display:grid;
      grid-template-columns: repeat(auto-fill, minmax(300px, 1fr));
      gap:24px;
    }
    .product{
      background: linear-gradient(180deg, rgba(255,255,255,0.03), rgba(255,255,255,0.01));
      border-radius:16px;
      padding:16px;
      border:1px solid rgba(201,162,74,0.15);
      transition: transform .18s ease, box-shadow .18s ease;
      position:relative;
      overflow:hidden;
    }
    .product::before {
      content:'';
      position:absolute;
      top:0;left:0;right:0;height:2px;
      background:linear-gradient(90deg, var(--gold), rgba(201,162,74,0.3));
    }
    .product:hover{ 
      transform: translateY(-8px); 
      box-shadow: 0 30px 70px rgba(201,162,74,0.1);
      border-color:rgba(201,162,74,0.3);
    }
    .product img{ 
      width:100%; 
      height:240px; 
      object-fit:cover; 
      border-radius:8px; 
      display:block;
      margin-bottom:12px;
      border:1px solid rgba(255,255,255,0.05);
    }
.product h3{ margin:10px 0 6px 0; font-size:16px; color:var(--accent); }
    .price{ 
      font-weight:700; 
      color:var(--gold); 
      font-size:18px; 
      margin-bottom:12px;
      letter-spacing:0.5px;
    }
    .desc{ 
      color:var(--muted); 
      font-size:14px; 
      margin-bottom:14px; 
      min-height:42px;
      line-height:1.5;
    }
.product .meta{ display:flex; justify-content:space-between; align-items:center; gap:10px; }
    .product .meta .btn-sm { 
      padding:10px 14px; 
      border-radius:8px; 
      cursor:pointer; 
      border:none; 
      background:rgba(255,255,255,0.03); 
      color:var(--accent); 
      font-weight:600;
      transition:all 0.2s ease;
    }
    .product .meta .btn-sm:hover {
      background:rgba(255,255,255,0.08);
    }
    .product .meta .btn-sm.buy{ 
      background: linear-gradient(180deg, rgba(201,162,74,0.2), rgba(201,162,74,0.1)); 
      color:var(--gold); 
      border:1px solid rgba(201,162,74,0.25);
    }
    .product .meta .btn-sm.buy:hover {
      background: linear-gradient(180deg, rgba(201,162,74,0.25), rgba(201,162,74,0.15));
    }
/* Cart Drawer */
  .cart-drawer{
    position: fixed;
    right: 18px;
    bottom: 18px;
    width: 360px;
    max-width: calc(100% - 36px);
    background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
    border-radius:14px;
    box-shadow: 0 30px 70px rgba(0,0,0,0.6);
    border:1px solid rgba(255,255,255,0.04);
    overflow:hidden;
    transform: translateY(30px) scale(0.98);
    opacity:0;
    visibility:hidden;
    transition: all .25s ease;
    z-index:100000;
  }
  .cart-drawer.open{ transform: translateY(0) scale(1); opacity:1; visibility:visible; }
  .cart-header{ padding:14px 16px; display:flex; justify-content:space-between; align-items:center; border-bottom:1px solid rgba(255,255,255,0.03); }
  .cart-list{ max-height:360px; overflow:auto; padding:12px 16px; }
  .cart-item{ display:flex; gap:12px; align-items:center; padding:10px 0; border-bottom:1px dashed rgba(255,255,255,0.02); }
  .cart-item img{ width:62px; height:62px; object-fit:cover; border-radius:8px; }
  .cart-item .info{ flex:1 }
  .cart-item .qty{ display:flex; gap:6px; align-items:center; }
  .cart-footer{ padding:12px 16px; border-top:1px solid rgba(255,255,255,0.03); display:flex; justify-content:space-between; align-items:center; gap:10px; }
  .total{ font-weight:700; color:var(--gold); }

  /* Checkout modal */
  .overlay {
    position: fixed; inset:0; background: rgba(0,0,0,0.6); display:none; align-items:center; justify-content:center; z-index:100001;
  }
  .checkout-card{
    width:100%; max-width:720px; background: linear-gradient(180deg, rgba(255,255,255,0.02), rgba(255,255,255,0.01));
    border-radius: 12px; padding:18px; color:var(--accent);
  }
  .grid-2{ display:grid; grid-template-columns:1fr 1fr; gap:12px; }
  label{ font-size:13px; color:var(--muted); display:block; margin-bottom:6px; }
  input, select, textarea{
    width:100%; padding:10px; border-radius:8px; border:1px solid rgba(255,255,255,0.04); background:transparent; color:var(--accent);
    outline:none; font-size:14px;
  }
  .small{ font-size:13px; color:var(--muted) }

  /* Footer */
  footer{ margin-top:28px; text-align:center; color:var(--muted); font-size:13px; }

  /* Responsivity */
  @media (max-width:980px){
    .hero{ grid-template-columns: 1fr; }
    .products{ grid-template-columns: repeat(2,1fr) }
    .cart-drawer{ width:92%; right:4%; left:4%; bottom:18px; }
  }
  @media (max-width:600px){
    .products{ grid-template-columns: 1fr }
    header{ gap:8px; flex-direction:column; align-items:flex-start }
    .header-actions{ width:100%; justify-content:space-between }
    .search input{ width:100px }
  }

  /* small helpers */
  .muted{ color:var(--muted); font-size:13px }
</style>
</head>
<body>
  <div class="site" role="main">
    <header>
      <div class="brand" aria-hidden="false">
        <div class="logo" aria-hidden="true"><span>A</span></div>
        <div>
        <h1>Rolex Collection</h1>
        <p>Official Rolex watches — Swiss precision since 1905</p>
</div>
      </div>

      <div class="header-actions" role="region" aria-label="Header Aktionen">
        <div class="search" role="search" aria-label="Produkte suchen">
          <svg width="16" height="16" viewBox="0 0 24 24" fill="none" style="opacity:.8"><path d="M21 21l-4.35-4.35" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round"/><circle cx="11" cy="11" r="6" stroke="currentColor" stroke-width="2"/></svg>
          <input id="searchInput" placeholder="Suche Uhren..." aria-label="Suche Uhren">
        </div>

        <button class="btn secondary" id="btn-wishlist" title="Favoriten">Favoriten</button>
        <button class="btn" id="btn-cart" aria-haspopup="dialog" aria-controls="cartDrawer">Warenkorb <span id="cartCount" class="cart-count">0</span></button>
      </div>
    </header>

    <!-- HERO -->
    <section class="hero" aria-label="Aurelux Kollektion">
      <div class="hero-card" role="region" aria-labelledby="heroTitle">
        <h2 id="heroTitle">Oyster Perpetual — The Rolex Legacy</h2>
        <p>Entdecke die offizielle Rolex Kollektion. Schweizer Präzision, zeitloses Design und unvergleichliche Qualität seit 1905.</p>
<div class="hero-cta">
          <button class="btn" id="shopAllBtn">Kollektion ansehen</button>
          <button class="btn secondary" id="learnBtn">Unsere Geschichte</button>
        </div>
      </div>
    <div class="hero-image" aria-hidden="true">
      <img src="https://content.rolex.com/dam/watches/family-pages/datejust/datejust-0001-hol-0001.jpg" alt="Rolex Datejust Uhr">
    </div>
</section>

    <!-- Produktliste -->
    <section>
      <h3 style="margin:0 0 12px 0; color:var(--muted)">Featured Kollektion</h3>
      <div class="products" id="productGrid" aria-live="polite">
        <!-- Produkte werden dynamisch eingefügt -->
      </div>
    </section>

    <footer>
      © <span id="year"></span> Aurelux Watches — All rights reserved. Designed with ❤️
    </footer>
  </div>

  <!-- Cart Drawer -->
  <aside id="cartDrawer" class="cart-drawer" aria-hidden="true" aria-label="Warenkorb">
    <div class="cart-header">
      <strong>Warenkorb</strong>
      <button id="closeCart" class="btn secondary" aria-label="Warenkorb schließen">Schließen</button>
    </div>
    <div class="cart-list" id="cartList" role="list">
      <!-- items -->
    </div>
    <div class="cart-footer">
      <div>
        <div class="small">Zwischensumme</div>
        <div class="total" id="cartTotal">0,00 €</div>
      </div>
      <div style="margin-left:auto">
        <button class="btn" id="checkoutBtn">Zur Kasse</button>
      </div>
    </div>
  </aside>

  <!-- Checkout Overlay -->
  <div class="overlay" id="overlay" aria-hidden="true">
    <div class="checkout-card" role="dialog" aria-modal="true" aria-labelledby="checkoutTitle">
      <h2 id="checkoutTitle">Checkout</h2>
      <p class="muted">Beachte: Dies ist ein Demo-Checkout. Es werden keine echten Zahlungen verarbeitet.</p>

      <form id="checkoutForm" onsubmit="return false;">
        <div style="display:flex;gap:12px;flex-wrap:wrap;margin-top:12px">
          <div style="flex:1;min-width:240px">
            <label for="name">Voller Name</label>
            <input id="name" required placeholder="Max Mustermann">
          </div>
          <div style="flex:1;min-width:200px">
            <label for="email">E-Mail</label>
            <input id="email" type="email" required placeholder="mail@example.com">
          </div>
        </div>

        <div style="margin-top:12px">
          <label for="address">Lieferadresse</label>
          <input id="address" required placeholder="Straße 12, 1010 Wien">
        </div>

        <div style="display:flex;gap:12px;margin-top:12px;align-items:end;flex-wrap:wrap">
          <div style="flex:1;min-width:160px">
            <label for="city">Ort</label>
            <input id="city" required placeholder="Wien">
          </div>
          <div style="width:130px">
            <label for="zip">PLZ</label>
            <input id="zip" required placeholder="1010">
          </div>
          <div style="width:160px">
            <label for="country">Land</label>
            <select id="country"><option>Österreich</option><option>Deutschland</option><option>Schweiz</option><option>Andere</option></select>
          </div>
        </div>

        <div style="margin-top:16px;display:flex;gap:12px;justify-content:flex-end">
          <button id="cancelCheckout" class="btn secondary" type="button">Abbrechen</button>
          <button id="submitOrder" class="btn" type="button">Kostenpflichtig bestellen</button>
        </div>
      </form>
    </div>
  </div>

<script>
  // Product data (demo). Bilder frei verwendbar (Unsplash). Du kannst später eigene Bilder einsetzen.
  const PRODUCTS = [
    {
      id: 'RLX-001',
      title: 'Rolex Daytona Platinum',
      price: 67900,
      desc: 'Platin-Gehäuse, exklusives Eisschwarz-Zifferblatt, Cerachrom-Lünette.',
      img: 'https://content.rolex.com/dam/watches/hubs/all-watches/collection/submariner/m126610ln-0001/model-page/m126610ln-0001_0010_rolex.jpg'
    },
    {
      id: 'RLX-002',
      title: 'Rolex Submariner Date',
      price: 11250,
      desc: 'Klassische Taucheruhr, 300m Wasserdicht, 18k Gold & Stahl.',
      img: 'https://content.rolex.com/dam/watches/hubs/all-watches/collection/gmt-master-ii/m126710blnr-0002/model-page/m126710blnr-0002_0010_rolex.jpg'
    },
    {
      id: 'RLX-003',
      title: 'Rolex GMT-Master II',
      price: 14300,
      desc: 'Dual-Time-Funktion, Oystersteel & Everose Gold, Saphirglas.',
      img: 'https://content.rolex.com/dam/watches/hubs/all-watches/collection/day-date/m228239-0038/model-page/m228239-0038_0010_rolex.jpg'
    },
    {
      id: 'RLX-004',
      title: 'Rolex Day-Date 40',
      price: 45800,
      desc: 'Präsidentenarmband, 18k Weißgold, Smaragd-Zifferblatt.',
      img: 'https://content.rolex.com/dam/watches/hubs/all-watches/collection/yacht-master/m226659-0002/model-page/m226659-0002_0010_rolex.jpg'
    },
    {
      id: 'RLX-005',
      title: 'Rolex Yacht-Master 42',
      price: 38600,
      desc: 'Platin-Gehäuse, mattschwarzes Zifferblatt, rutschfestes Oysterflex-Armband.',
      img: 'https://content.rolex.com/dam/watches/hubs/all-watches/collection/sky-dweller/m336934-0003/model-page/m336934-0003_0010_rolex.jpg'
    },
    {
      id: 'RLX-006',
      title: 'Rolex Sky-Dweller',
      price: 51900,
      desc: 'Jahreskalender & Dual-Time, 18k Roségold, Fluted Bezel.',
      img: 'https://content.rolex.com/dam/watches/hubs/all-watches/collection/datejust/m126203-0019/model-page/m126203-0019_0010_rolex.jpg'
    },
    {
      id: 'RLX-007',
      title: 'Rolex Datejust 36',
      price: 8900,
      desc: 'Ewige Eleganz, 18k Gelbgold, Jubilé-Armband, Diamantenindizes.',
      img: 'https://content.rolex.com/dam/watches/hubs/all-watches/collection/cosmograph-daytona/m116505-0016/model-page/m116505-0016_0010_rolex.jpg'
    },
    {
      id: 'RLX-008',
      title: 'Rolex Cosmograph Rainbow',
      price: 129000,
      desc: 'Exklusiv: Regenbogen-Saphire, 18k Roségold, Perpetual-Kaliber.',
      img: 'https://content.rolex.com/dam/watches/hubs/all-watches/collection/pearlmaster/m86285-0001/model-page/m86285-0001_0010_rolex.jpg'
    },
    {
      id: 'RLX-009',
      title: 'Rolex Pearlmaster 39',
      price: 75800,
      desc: 'Luxuriöses Damenmodell, 18k Weißgold mit Diamantenbesatz.',
      img: 'https://content.rolex.com/dam/watches/hubs/all-watches/collection/explorer/m224270-0001/model-page/m224270-0001_0010_rolex.jpg'
}
  ];
// utilities
  const formatPrice = (centsOrEuro) => {
    // INPUT prices are in whole euros above
    return new Intl.NumberFormat('de-DE', { style:'currency', currency:'EUR' }).format(centsOrEuro);
  };

  // render products
  const grid = document.getElementById('productGrid');
  function renderProducts(list = PRODUCTS){
    grid.innerHTML = '';
    list.forEach(p => {
      const card = document.createElement('div');
      card.className = 'product';
      card.innerHTML = `
        <img src="${p.img}" alt="${p.title}">
        <h3>${p.title}</h3>
        <div class="price">${formatPrice(p.price)}</div>
        <div class="desc">${p.desc}</div>
        <div class="meta">
          <button class="btn-sm" data-id="${p.id}" aria-label="Details ${p.title}">Details</button>
          <button class="btn-sm buy" data-add="${p.id}" aria-label="In den Warenkorb ${p.title}">In den Warenkorb</button>
        </div>
      `;
      grid.appendChild(card);
    });
  }
  renderProducts();

  // Cart logic
  let CART = JSON.parse(localStorage.getItem('aurelux_cart') || '[]');

  const cartBtn = document.getElementById('btn-cart');
  const cartDrawer = document.getElementById('cartDrawer');
  const cartList = document.getElementById('cartList');
  const cartCount = document.getElementById('cartCount');
  const cartTotalEl = document.getElementById('cartTotal');
  const closeCart = document.getElementById('closeCart');
  const checkoutBtn = document.getElementById('checkoutBtn');
  const overlay = document.getElementById('overlay');

  function saveCart(){ localStorage.setItem('aurelux_cart', JSON.stringify(CART)); }
  function updateCartDisplay(){
    cartCount.textContent = CART.reduce((s,i)=>s+i.qty,0);
    cartList.innerHTML = '';
    if(CART.length === 0){
      cartList.innerHTML = '<div class="muted" style="padding:18px;text-align:center">Dein Warenkorb ist leer.</div>';
      cartTotalEl.textContent = formatPrice(0);
      return;
    }
    let total = 0;
    CART.forEach(item=>{
      const product = PRODUCTS.find(p=>p.id===item.id);
      const row = document.createElement('div');
      row.className = 'cart-item';
      row.innerHTML = `
        <img src="${product.img}" alt="${product.title}">
        <div class="info">
          <div style="display:flex;justify-content:space-between;align-items:center">
            <div><strong style="font-size:14px">${product.title}</strong><div class="muted" style="font-size:12px">${formatPrice(product.price)}</div></div>
            <div style="text-align:right">
              <div class="qty">
                <button class="btn secondary btn-dec" data-id="${item.id}">−</button>
                <span style="padding:0 8px">${item.qty}</span>
                <button class="btn secondary btn-inc" data-id="${item.id}">+</button>
              </div>
              <div style="margin-top:6px"><button class="btn secondary btn-remove" data-id="${item.id}">Entfernen</button></div>
            </div>
          </div>
        </div>
      `;
      cartList.appendChild(row);
      total += product.price * item.qty;
    });
    cartTotalEl.textContent = formatPrice(total);
  }

  function openCart(){
    cartDrawer.classList.add('open');
    cartDrawer.setAttribute('aria-hidden','false');
    updateCartDisplay();
  }
  function closeCartFn(){
    cartDrawer.classList.remove('open');
    cartDrawer.setAttribute('aria-hidden','true');
  }

  cartBtn.addEventListener('click', openCart);
  closeCart.addEventListener('click', closeCartFn);

  // add to cart
  document.body.addEventListener('click', (e)=>{
    const add = e.target.closest('[data-add]');
    if(add){
      const id = add.getAttribute('data-add');
      const exists = CART.find(i=>i.id===id);
      if(exists) exists.qty++;
      else CART.push({ id, qty: 1 });
      saveCart();
      updateCartDisplay();
      openCart();
      return;
    }
    const dec = e.target.closest('.btn-dec');
    if(dec){
      const id = dec.getAttribute('data-id');
      const item = CART.find(i=>i.id===id);
      if(item){
        item.qty = Math.max(0, item.qty - 1);
        if(item.qty === 0) CART = CART.filter(i=>i.id!==id);
        saveCart(); updateCartDisplay();
      }
      return;
    }
    const inc = e.target.closest('.btn-inc');
    if(inc){
      const id = inc.getAttribute('data-id');
      const item = CART.find(i=>i.id===id);
      if(item){ item.qty++; saveCart(); updateCartDisplay(); }
      return;
    }
    const rem = e.target.closest('.btn-remove');
    if(rem){
      const id = rem.getAttribute('data-id');
      CART = CART.filter(i=>i.id!==id);
      saveCart(); updateCartDisplay();
      return;
    }
  });

  // checkout flow
  checkoutBtn.addEventListener('click', ()=>{
    if(CART.length === 0) { alert('Warenkorb ist leer.'); return; }
    overlay.style.display = 'flex';
    overlay.setAttribute('aria-hidden','false');
  });
  document.getElementById('cancelCheckout').addEventListener('click', ()=>{
    overlay.style.display = 'none';
    overlay.setAttribute('aria-hidden','true');
  });

  document.getElementById('submitOrder').addEventListener('click', ()=>{
    // simple validation
    const name = document.getElementById('name').value.trim();
    const email = document.getElementById('email').value.trim();
    const address = document.getElementById('address').value.trim();
    if(!name || !email || !address){ alert('Bitte fülle Name, E-Mail und Adresse aus.'); return; }

    // "process" the order (demo)
    const order = {
      id: 'AX' + Date.now(),
      date: new Date().toLocaleString(),
      items: CART,
      total: CART.reduce((s,i)=>(s + (PRODUCTS.find(p=>p.id===i.id).price * i.qty)),0),
      customer: { name, email, address }
    };

    // clear cart
    CART = [];
    saveCart();
    updateCartDisplay();
    overlay.style.display = 'none';
    overlay.setAttribute('aria-hidden','true');
    closeCartFn();

    // show confirmation (simple)
    setTimeout(()=> {
      alert('Danke für deine Bestellung! Bestellnummer: ' + order.id + '\n(Bei diesem Demo-Shop werden keine echten Zahlungen verarbeitet.)');
    }, 300);
  });

  // search filter
  document.getElementById('searchInput').addEventListener('input', (e)=>{
    const q = e.target.value.trim().toLowerCase();
    if(!q){ renderProducts(); return; }
    const filtered = PRODUCTS.filter(p => (p.title + ' ' + p.desc).toLowerCase().includes(q));
    renderProducts(filtered);
  });

  // shop all button scroll
  document.getElementById('shopAllBtn').addEventListener('click', ()=> {
    window.scrollTo({ top: document.getElementById('productGrid').offsetTop - 20, behavior:'smooth' });
  });

  // init
  document.getElementById('year').textContent = new Date().getFullYear();
  updateCartDisplay();

  // Accessibility: close overlay with ESC
  document.addEventListener('keydown', (e)=>{
    if(e.key === 'Escape'){
      overlay.style.display='none';
      overlay.setAttribute('aria-hidden','true');
      closeCartFn();
    }
  });

  // Progressive enhancements: prefer-reduced-motion
  if(window.matchMedia('(prefers-reduced-motion: reduce)').matches){
    document.querySelectorAll('*').forEach(el => { el.style.transition = 'none'; });
  }
</script>
</body>
</html>
